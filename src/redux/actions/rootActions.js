import { createAction } from '@reduxjs/toolkit';

export const setData = createAction('root/setData')
export const onGrabData = createAction('root/OnGrabData')
export const resetData = createAction('root/resetData')